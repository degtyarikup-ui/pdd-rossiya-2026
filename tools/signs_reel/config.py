#!/usr/bin/env python3
"""Дизайн-токены и тайминги роликов «Успей узнать знак».

Токены — зеркало `lib/core/constants/app_colors.dart` и `app_dimensions.dart`.
Правило проекта: цвета и размеры не выдумываем, а берём из приложения; всё,
что тут добавлено сверх токенов (тени, кегли), выведено из масштаба
1080 / 393 pt ≈ 2.75 — так кадр читается как экран приложения.
"""

from __future__ import annotations

# ── Холст ──────────────────────────────────────────────────────────────────
W, H = 1080, 1920
FPS = 30
SCALE = W / 393.0  # логический пиксель Flutter → пиксель кадра

# ── Цвета (AppColors) ──────────────────────────────────────────────────────
ACCENT = (5, 116, 248)        # #0574F8
LIGHT_ACCENT = (232, 242, 254)  # #E8F2FE
PRIMARY_TEXT = (18, 18, 18)   # #121212
SECONDARY_TEXT = (161, 166, 183)  # #A1A6B7
BACKGROUND = (248, 249, 250)  # #F8F9FA (homeScreenBackground)
WHITE = (255, 255, 255)
GREEN = (52, 168, 83)         # #34A853
GREEN_SURFACE = (232, 245, 233)  # #E8F5E9
RED = (234, 67, 53)           # #EA4335
RED_SURFACE = (255, 235, 238)  # #FFEBEE
GOLD = (255, 165, 60)         # #FFA53C
GOLD_SURFACE = (255, 241, 230)  # #FFF1E6
GRAY = (239, 240, 244)        # #EFF0F4
DIVIDER = (232, 232, 232)     # #E8E8E8

# ── Геометрия кадра ────────────────────────────────────────────────────────
CARD_RADIUS = round(16 * SCALE)      # 44
CHIP_RADIUS = round(12 * SCALE)      # 33
SMALL_RADIUS = round(8 * SCALE)      # 22
SCREEN_PADDING = round(20 * SCALE)   # 55

# Шапки нет: название категории и чип таймера ничего не добавляли к загадке.
# Время показывает только полоса внизу карточки — цветом, а не цифрой.

# Карточка знака
CARD_X = 60
CARD_W = W - 2 * CARD_X        # 960
CARD_Y = 280
CARD_H = 1160
SIGN_BOX = 560                 # максимальная сторона картинки знака
SIGN_CENTER_DY = 420           # центр знака от верха карточки
DIVIDER_DY = 780               # разделитель от верха карточки
DIVIDER_INSET = 90
NUMBER_DY = 845                # номер знака по ГОСТ (синий, как в приложении)
NUMBER_SIZE = 50
ANSWER_DY = 985                # центр блока ответа от верха карточки
ANSWER_SIZE_MAX = 72
ANSWER_SIZE_MIN = 42
ANSWER_MAX_LINES = 2
ANSWER_LEADING = 1.12          # плотнее: при 1,2 две строки расползаются
QUESTION_MARK_SIZE = 170

# Полоса времени внутри карточки
BAR_INSET = 80
BAR_DY_FROM_BOTTOM = 70
BAR_H = 30

# Ряд номеров знаков внизу — как чипы вопросов в приложении
DOTS_Y = 1680

DOTS_H = 76
DOTS_GAP = 16

# Пояснение — ровно посередине между низом карточки и рядом номеров
NOTE_CY = (CARD_Y + CARD_H + DOTS_Y) // 2   # 1560
NOTE_SIZE = 38
NOTE_MAX_LINES = 2

# Финал: сетка знаков выпуска и рекламная плашка.
# Всё поднято выше середины: нижние ~400 px забирает интерфейс TikTok /
# Shorts / Reels — подпись, аватар, кнопки.
OUTRO_GRID_Y = 160
OUTRO_GRID_BOTTOM = 1180

# Субтитры (интро и финал): 1–2 слова на синей плашке
SUB_SIZE_MAX = 110
SUB_SIZE_MIN = 60
SUB_PAD_X = 56
SUB_PAD_Y = 34
SUB_WORDS = 2
INTRO_SUB_Y = 960     # центр плашки в интро

# Рекламная плашка в финале (анимированный WebM с альфой)
BANNER_W = 1000
BANNER_CY = 1380      # центр плашки по вертикали

# Фон: сгенерированная сцена со знаками. В фазах загадки её забивает
# брендовый синий, иначе белая карточка на пёстрой картинке теряется.
BG_BLUE_ALPHA = 0.82
# В интро и финале сцена идёт без затемнения: карточки там белые и читаются
# сами, а притушка съедала свет со всего кадра.
BG_DARK_ALPHA = 0.0

# ── Тайминги одного знака (сек) ────────────────────────────────────────────
T_SHOW = 0.8       # знак появился, отсчёт ещё не идёт
T_COUNTDOWN = 3.0  # отсчёт
T_REVEAL_TAIL = 0.9  # хвост после того, как диктор договорил
T_REVEAL_MIN = 2.0   # ответ висит не меньше этого, иначе не прочитать
# Переход — ровно та же анимация, что между вопросами в приложении
# (QuestionSwipeMotion: 280 мс, easeOutCubic).
T_SWIPE = 0.28

# Пороги цвета полосы/таймера
BAR_GREEN_ABOVE = 0.60
BAR_GOLD_ABOVE = 0.30

# ── Интро / аутро ──────────────────────────────────────────────────────────
INTRO_TAIL = 0.5
OUTRO_TAIL = 0.8
# Финал держится дольше озвучки: зритель сверяет свои ответы по сетке знаков.
OUTRO_MIN = 5.0

# ── Аудио ──────────────────────────────────────────────────────────────────
SR = 24000               # частота дискретизации TTS Gemini
TICK_PERIOD = 0.5
TICK_LEN = 0.03
TICK_DECAY = 0.006
TICK_GAIN = 0.20
TICK_FREQ_HI = 1400.0
TICK_FREQ_LO = 1100.0
CHIME_GAIN = 0.18        # звук раскрытия ответа
